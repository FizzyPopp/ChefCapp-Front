import 'package:chef_capp/index.dart';
import 'dart:math';

class User {
  // unit preferences
  // dietary restrictions
  // look at db for more
  final ID _id;
  final String _email;
  User(this._id, this._email);
}